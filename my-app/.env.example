DATABASE_URL=
# At least 32 random characters; setup-auth.mjs generates this locally if missing.
AUTH_SECRET=
# Exact browser origin, no trailing slash. Use HTTPS in production.
AUTH_ORIGIN=http://localhost:3000

# Optional executable path (not a shell command). Defaults to ai/.venv Python, then python on PATH.
# PYTHON_EXECUTABLE=C:/path/to/python.exe
